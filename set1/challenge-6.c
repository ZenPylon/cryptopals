#include <stdlib.h>
#include <stdio.h>
#include "encodings.h"

int main(int argc, char **argv)
{
    init_base64_module();
    
    // Load file

    // Decode base64 into unencoded (but encrypted) bytes


    return 0;
}